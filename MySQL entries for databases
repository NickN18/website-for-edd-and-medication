CREATE TABLE mediPerUser (userID INT UNSIGNED NOT NULL AUTO_INCREMENT, medication VARCHAR(255) NOT NULL, dosage VARCHAR(255) NOT NULL, dateToTake VARCHAR(255) NOT NULL, FOREIGN KEY (userID) REFERENCES users(userID));

ALTER TABLE mediPerUser DROP userID;

ALTER TABLE mediPerUser ADD userName VARCHAR(255);

